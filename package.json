{
    "private": true,
    "scripts": {
        "dev": "php artisan serve --host 0.0.0.0 --port 10000",
        "build": "./.render-build.sh"
    },
    "devDependencies": {
        "axios": "^1.1.2",
        "laravel-vite-plugin": "^0.7.2",
        "vite": "^4.0.0"
    }
}
